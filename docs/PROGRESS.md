# Ashfall — Progress Tracker

Single source of truth for delivery status. **How to use this doc:** update the Status column every merge, fill the Commit + Verification evidence columns at the moment a phase lands, and link PRs/commits here — if it is not reflected in this table, it did not ship. Do not track phase detail here; ROADMAP.md owns scope, this file owns state.

## Status legend

| Status | Meaning |
|--------|---------|
| TODO | Not started; no branch exists |
| IN PROGRESS | Active branch/worktree with commits |
| REVIEW | Code complete, awaiting review |
| TESTING | In verification (tests + live smoke) |
| DONE | Merged to origin master, CI green, evidence recorded below |
| BLOCKED | Cannot proceed; blocker noted in the row |

## Master table

| Phase | Quarter | Scope summary | FR refs | Status | Worktree/branch | Commit | Verification evidence (test cmd + result) | Date |
|-------|---------|---------------|---------|--------|-----------------|--------|-------------------------------------------|------|
| Architecture doc | pre-roadmap | Module map, data flows, D1-D15 duplication inventory, extension points, test strategy (docs/ARCHITECTURE.md) | n/a (docs) | DONE | master | 92349e1 | `npm run check && npm test` green (docs-only change); facts verified against code per doc header | 2026-08-22 |
| Competitor research | pre-roadmap | 19 OSS projects surveyed, B2B white-space analysis, positioning recommendations (docs/COMPETITORS.md) | n/a (docs) | DONE | master | b77acfd | `npm run check && npm test` green (docs-only change); licenses verified via web search Aug 2026 per doc header | 2026-08-22 |
| Docs tracker setup | pre-roadmap | artwork-ui-rework research + TODO tracker, fetch-assets CLI docs, session notes (docs/artwork-ui-rework/) | n/a (docs) | DONE | master | a868e54 | `npm run check && npm test` green | 2026-08-22 |
| Asset pipeline (WIP baseline) | pre-roadmap | Manifest-driven fetch logic, zip extraction, scatter math, TDD (tools/fetch-assets.mjs, src/tools/assetPipeline.js, src/tools/scatter.js, src/tools/zipExtract.js) | FR-CONTENT-01 | DONE | master | bff7fff | `npm run check && npm test` green — asset-pipeline.test.mjs, asset-pipeline-direct.test.mjs, scatter.test.mjs added (17 files / 39 tests total) | 2026-08-22 |
| Baseline flaky-test fix | pre-roadmap | Fix flaky phase4 assertion — count upgrade stacks, not distinct ids | FR-FAIR-03 | DONE | master | de77d54 | `npm run check && npm test` green x consecutive runs post-fix | 2026-08-22 |
| 1.1 LICENSE + NOTICE | Q1 | MIT LICENSE file, NOTICE attributions from assets/credits/, README license section | FR-CONTENT-02, FR-BIZ-01 | DONE | feat/p1.1-mit-license | 117226f | red-first TDD: `node --test test/license.test.mjs` 3 fail -> 3 pass; full gate green — `npm run check` ok, `npm test` 18 files / 42 tests pass, `npm run smoke` 8/8 | 2026-08-22 |
| 1.2 CI gate | Q1 | Syntax + test gate on every push (`npm run check && npm test`) — local gate established and enforced as merge habit | FR-OPS-02, FR-FAIR-03 | DONE | master | (pre-existing habit; hosted wiring tracked in 1.5) | `npm run check && npm test` green on master — 17 test files / 39 tests verified (ARCHITECTURE.md section 7) | 2026-08-22 |
| 1.3 Extract shared-sim duplication D2-D8 | Q1 | Pull SAFE-TO-EXTRACT mirrored blocks (progression bookkeeping, shop effects, burn DoT, enemy-hit resolution, projectile loop, pause wall, match reset) into shared modules used by GameRoom + LocalRoom | FR-FAIR-03, FR-CORE-05 | DONE | master (slices 4/4 done) | 9c7f89f, 709f4bd, c3adc18, b4cbee2 | Slice 1: red-first TDD — `node --test test/simLeveling.test.mjs` fail (ERR_MODULE_NOT_FOUND) -> 16/16 pass; full gate green (`npm run check` ok, `npm test` 19 files / 58 tests, `npm run smoke` 8/8); LOC GameRoom.js 1330 -> 1240 (-90), LocalRoom.js 892 -> 852 (-40), new src/shared/sim/leveling.js 196 lines. Slice 2: red-first TDD — `node --test test/simCombatBook.test.mjs` fail (ERR_MODULE_NOT_FOUND) -> 16/16 pass; full gate green — `npm run check` 70 files ok, `npm test` 20 files / 74 tests pass, `npm run smoke` OK — 8/8 incl. live WebSocket join; LOC GameRoom.js 1240 -> 1206 (-34), LocalRoom.js 852 -> 835 (-17), new src/shared/sim/combatBook.js 116 lines + src/shared/sim/shopEffects.js 78 lines. Sanctioned alignment #1 (design doc section 1, D4): LocalRoom now calls tickBurns OUTSIDE its `me.hp > 0` block so burns keep ticking while the local player is dead, matching GameRoom — previously unpinned by any test. Notes: one transient arena.test.mjs failure during a full-suite pass (ArenaRoom team-round-reset race at test line 145; ArenaRoom untouched by this slice) — suite green on immediate re-run and in isolation; LocalRoom boolean `_shopPicked` migrated to the sid-keyed Map shape per plan section 1 D3. Slice 3: red-first TDD — `node --test test/simProjectileLoop.test.mjs` fail (ERR_MODULE_NOT_FOUND for src/shared/sim/projectileLoop.js) -> 13/13 pass; full gate green — `npm run check` 72 files ok, `npm test` 21 files / 87 tests pass (x4 consecutive runs), `npm run smoke` OK — 8/8 incl. real server boot + live WebSocket join on :28175; LOC GameRoom.js 1206 -> 1181 (-25), LocalRoom.js 835 -> 816 (-19), new src/shared/sim/projectileLoop.js 93 lines + test/simProjectileLoop.test.mjs 312 lines; D6 loop delegates from both rooms via a simProjectiles ctx (onHitEnemy/onHitPlayer hooks); fireball burn handoff rides the same ctx through combatBook.startBurnFromProjectile; LR's inverted `!ownerIsPlayer` player branch unified onto the GR rule (owner projectiles hit OTHER players only) — unreachable dead code offline per plan section 1 D6, now pinned by unit tests; NO sanctioned alignments this slice, behavior identical; one uncaptured transient failure in the first full-suite pass (86/87), consistent with the known ArenaRoom race documented above (ArenaRoom untouched by this slice), then 4 consecutive green runs. Slice 4: red-first TDD — `node --test test/simMatchPhases.test.mjs` fail (ERR_MODULE_NOT_FOUND for src/shared/sim/matchPhases.js) -> 19/19 pass; full gate green — `npm run check` 74 files ok, `npm test` 22 files / 106 tests pass (x2 consecutive runs), `npm run smoke` OK — 8/8 incl. real server boot + live WebSocket join on :30871; LOC GameRoom.js 1181 -> 1141 (-40), LocalRoom.js 816 -> 780 (-36), new src/shared/sim/matchPhases.js 183 lines (D7 pauseGate + D8 resetMatchState) + stretch D1 activateWave added to src/shared/waves.js (+45 lines, sanctioned by design doc section 1 D1 "Decision: PARAMETERIZE" — attempted and shipped) + test/simMatchPhases.test.mjs 504 lines incl. boot-harness parity block; rooms delegate via a simPhases ctx (pauseBox/intermissionBox mutable holders, checkAutoPicks ordering pinned by unit test, per-side checkWinWhilePaused hook: GR full checkWinConditions(0), LR keeps its historical score-target-only block per plan section 1 D7 difference 2); room scalars pausedUntil/intermissionUntil migrated to {until} boxes (internal-only, no external readers); Sanctioned alignment #2 (design doc section 1, D8): BOTH rooms clear state.projectiles on match reset — GameRoom gains the clear LocalRoom always had (stale server projectiles previously resumed flying across playAgain); pinned end to end by the new integration assert post-playAgain projectiles.length === 0 on both sides | 2026-08-22 |
| 1.4 Flaky-test elimination | Q1 | Fix timing-dependent assertions; delete probe scripts flagged in ARCHITECTURE.md section 5.4 (probes already removed in d185d85) | FR-FAIR-03 | DONE | fix/p1.4-flaky-tests | fcef8fb, fe15c33, d587dd9 | Audit (read-only, isolated worktree): 3x suite green locally + 8 ranked findings. Deterministic red proofs for #1 (combat pre-impact 40ms margin — stall-injection fails old, passes new) and #2 (world.test.mjs unlink-before-flushAll leak — 2x-loop repro). Fixes #1-#6 landed (server-truth pendingMelee drain; flushAll-before-unlink + WorldTester.json cleanup + permanent regression assert; _resetForTests read isolation before grantXp; waitFor(existsSync,5000); combat skill/rear-contact polls 3000ms; multiplayer anim budget 1000->3000ms). Extra hardening after post-merge flake 2/5: PvP melee poll, killScore orb parking, A1 cooldown boundary <=800, bash cone intent-park (movement.js rotY snap root cause) — 12/12 stress green after. CI run 32530964445 success. Arena stretch: race NOT reproduced in 8 runs; waitMs(600)->waitFor hardening applied to arena.test.mjs round-win waits in follow-up commit. Gate: `npm test` x12 consecutive green + `npm run smoke` 8/8 | 2026-08-22 |
| 1.5 Playwright e2e in hosted CI | Q1 | browser.test.py promoted into pipeline; documented /metrics labels | FR-OPS-02 | DONE | master (feat/p1.5-browser-e2e) | (this merge) | Decision A: CI drives the maintained test/browser.test.py via setup-python + `pip install playwright` + `playwright install --with-deps chromium` — script is 460 lines of python-closure probes, not trivially portable, so the maintained script stays authoritative (only change: `E2E_URL` env with same default). New ci.yml `e2e` job runs parallel to verify: boots production entrypoint (`PORT=2567 node src/server/index.js`), polls /healthz 30s, runs all three flows. Local preflight (exact CI sequence on spare :2568): GREEN waves+world+pvp in 30.3s (< 10 min gate); red experiment (broken client boot in worktree copy of network.js, reverted) -> RED exit 1 with hud stuck `connecting…`. /metrics labels remain self-documented via Prometheus HELP/TYPE lines in http.js. Gate: deliberately broken client-wiring change fails e2e (proven locally; CI now enforces on every push) | 2026-08-22 |
| 2.1 OIDC/OAuth2 login option | Q2 | Server-side OIDC flow via env config, identity -> profile mapping, guest path untouched | FR-ID-02, FR-ID-01 | TODO | TBD | - | Gate: stub-provider integration test; existing guest tests pass unmodified | - |
| 2.2 Persistence adapter + Postgres driver (Cycle 20) | Q2 | persistence.js stays the facade with byte-identical json default; PERSISTENCE_DRIVER=postgres swaps backing to a `players` table (src/server/pgStore.js: lazy `import('pg')`, idempotent DDL, upsert writes, hard delete, keys()) PRELOADED into an in-memory cache at boot (persistenceReady() awaited by index.js before listen) so every synchronous room read path is unchanged — finalizes run inside the fixed-timestep step and must never await; writes stay debounced, cache-first, flushed to SQL; async seam exports loadPlayerAsync/savePlayerAsync/deletePlayerAsync/flushAllAsync; boot fail-fast without DATABASE_URL; README env rows + CI postgres service container w/ TEST_DATABASE_URL (docs/PRD-postgres-adapter.md) | FR-PERSIST-02, FR-PERSIST-01 | DONE | master | 0149927 | TDD: test/playerStore.test.mjs (raw storage contract vs json always + PostgresStore when TEST_DATABASE_URL set incl. non-object JSONB tolerance; facade debounce semantics overlay/cancel/no-resurrection; missing-DATABASE_URL fail-fast) + test/pgFacade.test.mjs (env-before-import boots facade over live pg: preload serves SYNC reads, debounce window not-yet-durable, flush durable, delete clears all layers) — red-first, verified against ephemeral local cluster :54329. Full gate green json-default — `npm run check` ok, `npm test` 221 pass / 0 fail / 1 env-gated skip (entire pre-existing suite UNMODIFIED = AC1 byte-identical), smoke 8/8 | 2026-08-22 |
| 2.3 Admin API + GDPR rights + audit log | Q2 | Authenticated export/delete/list endpoints, JSON-lines audit log, data-inventory documentation | FR-PERSIST-03, FR-PERSIST-04, FR-OPS-04 | TODO | TBD | - | Gate: export byte-complete vs load; deletion audited with actor+timestamp; no-token requests rejected | - |
| 2.5 Daily Gauntlet (retention) | Q2 | Seeded daily challenge run: date-hash modifiers via shared/sim/dailyRun.js, dedicated 'daily' room type, all-dead run finalize, streak + XP-reward persistence, GET /api/daily leaderboard, client Daily mode card + results banner (docs/PRD-daily-gauntlet.md) | FR-RET-01 | DONE | master | b08f14f | TDD: test/dailyRun.test.mjs (contract incl. month/year rollover streak math) + test/daily.test.mjs (modifier scaling, cross-room seeded-layout parity, waves untouched, /api/daily shape, all-dead finalize -> persistence + leaderboard). Full gate green — `npm run check` 78 files ok, `npm test` 24 files / 108 tests pass, `npm run smoke` 8/8 incl. real WS join; live probe joined 'daily' on :3999 (players 1, enemies 10 wave 1, Elite Guard mods) | 2026-08-22 |
| 2.22 Objective-Based Dailies (Cycle 18) | Q2+ | Mirror of cycle-17 objective weeklies on the daily pipeline: shared/sim/dailyRun.js pure additions (DAILY_OBJECTIVES table wave_5/wave_8/score_500/score_1200 inclusive thresholds; dailyObjectives(dateStrOrSeed) = 2 distinct LCG picks, non-mutating; evaluateDailyRun truth table; mergeDailyObjectives sticky keyed on .date — same-day done-once-true, new day replaces wholesale), GameRoom daily finalize stores merged objectives on the daily record before save, /api/daily exposes objectives [{id,description}] (predicates not leaked) + leaderboard rows carry objectivesDone (docs/PRD-daily-objectives.md) | FR-RET-01 | DONE | master | 05a49fe | TDD: test/dailyObjectives.test.mjs red-first (table shape, determinism + 2-distinct + numeric-seed + table-immunity across sampled days, inclusive boundary truth table, sticky/new-day merge semantics, live-server integration: seeded weak prior-day record proves stickiness through real all-dead finalize at wave 9/score 90, persisted blob matches evaluation, endpoint definitions + objectivesDone count). Full gate green — `npm run check` ok, `npm test` 212 tests pass 0 fail, `npm run smoke` 8/8. CI verify+e2e green on push (run 32582601082) | 2026-08-22 |
| 2.23 Combat Radar (Cycle 19) | Q2+ | Match-mode situational awareness (waves/daily/weekly had zero off-screen threat surface): shared/sim/radar.js pure projectRadar(entities, self, span) -> [{u,v,clamped}] self-centered canvas-normalized projection with direction-preserving rim clamping for beyond-span blips (zero imports, no DOM), src/ui/CombatRadar.js round HUD canvas (enemy red / ally tint blips, rim blips dimmed + stroked, self ring + crosshair; Minimap canvas pattern mirror), GameScene single lazy instance rendered only during live non-world matches, hidden on lobby/gameover/spectate; world-mode chunk minimap untouched (docs/PRD-combat-radar.md) | FR-RET-01 | DONE | master | df77347 | TDD: test/radar.test.mjs red-first 5 units (center exactness, axis edge mapping, corner projection deep-equal, rim clamp with clamped flag incl. far NW corner -> (0,0), empty input + determinism). Full gate green — `npm run check` ok, `npm test` 217 tests pass 0 fail, `npm run smoke` 8/8 incl. real WS join | 2026-08-22 |
| 2.24 Results Share Card (Cycle 21) | Q2+ | Post-game social loop: shared/sim/shareCard.js pure buildShareCard(run) -> {headline, stats[], text} deterministic + mode-aware (weekly carries objectivesDone/Total, daily carries streak, waves/arena omit challenge lines; absent fields never fabricate zeros) + shareText(card) clipboard string ending in game name, zero imports/no DOM; GameScene SHARE pill lazily created inside the gameover card on match-over only (hidden on death/respawn overlay), click copies via navigator.clipboard with COPIED ack + stopPropagation so overlay restart is not triggered; graceful COPY FAILED when clipboard unavailable (docs/PRD-share-card.md) | FR-RET-01 | DONE | master | b20871f | TDD: test/shareCard.test.mjs red-first 4 units (determinism deep-equal, mode-awareness incl. no-fabricated-lines, victory/defeat headline pinning per family, shareText single-occurrence digit-disjoint stat values + game name). Full gate green — `npm run check` 130 files ok, `npm test` 225 pass / 0 fail / 1 env-gated skip, `npm run smoke` 8/8 incl. real WS join | 2026-08-22 |
| R43 Stability soak + perf-gate flake fix (Cycle 22) | Q2+ | Post-sweep soak across cycles 18-21 + peer landings caught test/perfSurge.test.mjs failing intermittently under full-suite parallel contention (raw max tick 102.61ms vs 45ms assert; solo always green, soak runs 1/3 red vs run 2 clean) — root cause: wall-clock raw max includes OS preemption spikes from sibling test processes. Fix aligned with the gate's stated purpose: median<10 / p95<25 kept, raw-max assert replaced by p99<45ms + 250ms pathology ceiling | FR-FAIR-03 | DONE | master | 3915f65 | Evidence: pre-fix soak 3 runs -> fail/fail-pattern (1F/0F/1F) with perfSurge the only failure; post-fix 3 consecutive full suites 225 pass / 0 fail / 1 env-gated skip each; smoke 8/8 on :26615; solo perf sample median=0.35ms p95=0.87ms n=285 | 2026-08-22 |
| 2.25 Objective HUD (Cycle 23) | Q2+ | Completes the objectives loop: DAILY/WEEKLY_OBJECTIVES rows gain machine-readable {kind: wave|score, value} consistent with predicates, /api/daily + /api/weekly pass target through (predicates still never cross the wire), new pure shared/sim/objectivesHud.js objectiveProgress(targets, {wave,score}) mirrors server inclusive >= semantics; client surfaces goals for the first time — menu daily/weekly card subtitles append goal lines (graceful offline degrade untouched) + live in-match top-center chip with [x]/[ ] marks updated per frame from synced wave/score in daily/weekly modes only, hidden on lobby/gameover/spectate/world (docs/PRD-objective-hud.md) | FR-RET-01 | DONE | master | 44d4f63 | TDD: test/objectivesHud.test.mjs red-first 3 units (kind/value-vs-predicate boundary consistency both tables, inclusive boundaries + determinism, cross-evaluator agreement across sampled days AND weeks vs evaluateDailyRun/evaluateWeeklyRun). Full gate green — `npm run check` 132 files ok, `npm test` 229 pass / 0 fail / 1 env-gated skip, `npm run smoke` 8/8 incl. real WS join | 2026-08-22 |

| 2.20 Waves spectate + spectator counts | Q2 | GameRoom.onJoin spectator branch (mirror of ArenaRoom: presence 'spectating', no seat/capacity/finalize-eligibility, onLeave guard) + /api/rooms spectators field per roomId (single presence scan) + client SPECTATE buttons extended from arena-only to all room rows + 👁N count chip in match meta (docs/PRD-waves-spectate.md) | FR-SOC-03 | DONE | master | 701a267 | TDD: test/wavesSpectate.test.mjs (playing waves + spectator: no seat, spectators>=1 in listing, canJoin unchanged; daily finalize excludes spectator — results.length 1, streak persisted; leave drops count). Full gate green — `npm run check` 120 files ok, `npm test` 208 tests pass; live probe waves room shows spectators:1 over WS, smoke 8/8 | 2026-08-22 |
| 2.19 Stability soak (post-Q2-feature sweep) | Q2 | Hardening pass across all 14 shipped features: full `npm test` x3 consecutive + Playwright real-browser e2e x2 on one server boot; zero flakes, zero failures — no code changes required | FR-FAIR-03 | DONE | master | 788c9a0 (evidence-only) | Evidence: runs 1-3 → 207/207 each; e2e runs 1-2 → GREEN waves+world+pvp; prior transient mid-run failures attributed to concurrent-session file swaps (documented in 2.7/2.8 rows), not reproducible solo | 2026-08-22 |
| 2.18 Air-gapped vendoring | Q2 | tools/vendor-assets.mjs fetches pinned jsdelivr files into committed assets/vendor/ (14 files incl. transitive three/addons imports + manifest.json), idempotent --force refetch; VENDORED_ASSETS=1 rewrites served index.html CDN refs → /vendor/* (mtime-cached) + serves vendor dir w/ JS MIME + immutable cache; traversal-safe; default mode byte-identical; README offline-deploy recipe (docs/PRD-airgap-vendoring.md) | FR-OPS-08 | DONE | master | 31990b9 | TDD: test/vendored.test.mjs (default keeps CDN; vendored page zero jsdelivr refs; all 14 manifest entries 200 JS-MIME immutable byte-exact vs disk; unknown+traversal 404). Full gate green — `npm run check` 119 files ok, `npm test` 207 tests pass; smoke default-mode 8/8 | 2026-08-22 |
| 2.17 Admin API + GDPR + audit | Q2 | ADMIN_TOKEN-guarded routes (unset → 404 zero surface; timingSafeEqual sha256 digest compare): GET /api/admin/players (malformed-tolerant scan), GET .../:name byte-complete export, DELETE .../:name durable via new persistence.cancelPendingSave(name) hook (debounced writer cannot resurrect), GET /api/admin/audit tail; src/server/auditLog.js JSONL {ts,actor,action,target,outcome} appended before responding incl. failures (docs/PRD-admin-api.md) | FR-PERSIST-03, FR-PERSIST-04 | DONE | master | 3fe1a6a | TDD: test/adminApi.test.mjs (AC1 feature-off 404 mid-run flip; AC2 401 x4; list shape + malformed tolerance + no-audit-on-list; AC3 export deep-equals file; AC4 delete durability across full debounce window + repeat-404; AC5 audit entries export-ok/delete-ok/delete-fail w/ ts+actor; digest-compare unit). Full gate green — `npm run check` 118 files ok, `npm test` 206 tests pass; live probe 401-noauth/200-auth + smoke 8/8 | 2026-08-22 |
| 2.16 Verified-name join guard | Q2 | Closes 2.15's deferred hijack vector: assertNameJoinable(name,options) in auth/oidc.js rejects guest joins on oidcSub-bound names w/ Colyseus error 4103 'name locked by verified account'; verified sessions pass via single-use 60s join tickets (mintTicketForSession, GET /auth/join-token); guards in GameRoom/ArenaRoom/WorldRoom onJoin before seat creation (spectators bypass; grace reconnects re-gate); feature-off deployments byte-identical; client threads ticket via fetchJoinTicket() into joinGame/joinWorld when /api/me reports verified (docs/PRD-name-guard.md) | FR-ID-01 | DONE | master | 5db6c91 | TDD: test/nameGuard.test.mjs (all 3 rooms reject no-ticket/forged-ticket 4103 no seat leaked; join-token 401-anon/200-session; minted ticket admits exactly one join; unbound name unaffected; disabled flag skips guard; unit contract). Full gate green — `npm run check` 116 files ok, `npm test` 205 tests pass; smoke 8/8 | 2026-08-22 |
| 2.15 OIDC login option | Q2 | BFF-pattern Authorization-Code+PKCE via openid-client@6: /auth/login/start (state+nonce+verifier stash, 5min TTL single-use) → IdP → /auth/callback (token exchange server-side, iss/aud/exp/nonce validation, sub→player binding writes oidcSub into data/players/<name>.json with both-direction collision rejection 409) → opaque HttpOnly SameSite=Lax session cookie; /api/me {name,verified}; /auth/logout; feature OFF unless OIDC_* env set (guest path byte-identical); client SSO link + VERIFIED badge w/ ?login=ok cleanup; provider tokens never serialized (docs/PRD-oidc-login.md) | FR-ID-02 | DONE | master | 726b4c6 | TDD: test/oidc.test.mjs — in-process stub IdP (RSA JWKS + discovery + PKCE /authorize + RS256 /token) driving full browser-less flow: disabled-default inertness, happy path w/ persisted oidcSub, both collision directions 409, stash single-use+expiry, logout, zero token leakage across 23 response bodies. Full gate green — `npm run check` 115 files ok, `npm test` 204 tests pass; live probe confirms disabled-mode inertness (/api/me 404, healthz 200), smoke 8/8. Known deferral: room-join guest-collision enforcement (next cycle) | 2026-08-22 |
| 2.14 Offline progression checkpoints | Q2 | Endless local play never fires endMatch → career/achievements were dead in the player's primary mode. Fix: shared/sim/localCareer.js (localStorage-backed {runs,bestWave,bestScore}, corrupt-JSON tolerant) + LocalRoom endless-only per-wave-clear checkpoint hook (onCheckpoint callback, room stays storage-agnostic) + incremental evaluateAchievements emission (once per new id) + client save/render PERSONAL BEST line on login card, refreshed at all menu-return sites (docs/PRD-offline-progression.md) | FR-RET-04 | DONE | master | 5ea1919 | TDD: test/localCareer.test.mjs 4 unit (round-trip, corrupt/junk JSON graceful, monotonic maxes non-mutating, null-seed) + test/endlessCheckpoints.test.mjs (two clears → two checkpoints bestWave 1→2, achievements emitted once per id, legacy default never fires onCheckpoint). Full gate green — `npm run check` 113 files ok, `npm test` 203 tests pass; smoke 8/8 | 2026-08-22 |
| 2.13 Arena spectate | Q2 | Seatless spectators: ArenaRoom.onJoin spectator branch (no PlayerState/capacity/teams/auto-start; presence mode 'spectating'; onLeave guard), network.spectateMatch via SDK joinById, client SPECTATE buttons on arena rows in LIVE MATCHES + follow-cam rig (first remotePlayer w/ local-rig constants, orbit fallback) + LEAVE pill with full menu reset (docs/PRD-arena-spectate.md) | FR-SOC-02 | DONE | master | 7ae1d7b | TDD: test/spectate.test.mjs (playing-phase: no seat + presence 'spectating' + state visible; lobby-phase: no countdown started; leave cleanup). Full gate green — `npm run check` 110 files ok, `npm test` 196 tests pass; smoke 8/8; live probe rooms listing + WS flows green | 2026-08-22 |
| 2.12 Achievements | Q2 | Pure predicate engine (shared/sim/achievements.js, 10 entries over persisted career{runs,victories,bestWave,bestScore}+daily.streak+weekly.bestScore) evaluated at every finalize site in GameRoom (career/daily/weekly) with LocalRoom mirror; newIds-only 'achievementsUnlocked' broadcast persisted as player.achievements[]; client gold toast w/ queue + star float (docs/PRD-achievements.md). Also this session: user-reported offline fix — LocalRoom({endless:true}) disables targetScore win + finale arc for unlimited offline waves (7bfd9d9) | FR-RET-03 | DONE | master | 604cc26 | TDD: test/achievements.test.mjs 5 unit (shape/boundaries-inclusive/no-dup/stable-order/field-pinning) + test/achievementsIntegration.test.mjs (one broadcast first_run+wave_5, file flush, replay silent, LocalRoom parity) + test/endlessLocal.test.mjs 3 (endless score/finale skip, legacy default preserved). Full gate green — `npm run check` 109 files ok, `npm test` 195 tests pass; smoke 8/8 | 2026-08-22 |
| 2.11 Weekly Gauntlet | Q2 | ISO-week-seeded challenge runs reusing the daily pipeline: shared/sim/weeklyRun.js (utcWeekKey ISO-8601 Mon-start, LCG seed, 3-row stacked modifiers hp xN multiplicative + speed cap 2, score-ladder rewards, mergeWeekly same-week-max) — deliberately NO streak; 'weekly' room type isolated like daily; finalize writes player.weekly{week,bestScore,lastPlayed} + broadcast dailyResult kind:'weekly'; GET /api/weekly leaderboard; client Weekly mode card + subtitle fetch + banner header swap (docs/PRD-weekly-gauntlet.md). Room/client hunks co-landed via parallel session's R16-R18 commits | FR-RET-02 | DONE | master | ca95762 | TDD: test/weekly.test.mjs 4 unit (ISO week math incl. year-boundary + Sun/Mon edge, determinism, stack >= single rows, ladder monotonic, merge rules) + test/weeklyIntegration.test.mjs (stacked hp vs plain, wipe persistence, /api/weekly shape+pickup). Full gate green — `npm run check` 103 files ok, `npm test` 181 tests pass; live probe :3999 joined weekly room playable (10 enemies), /api/weekly returns W34 stack "Swift + Vampiric + Bulwark" hp 3.13x, smoke 8/8 | 2026-08-22 |
| 2.10 Live match browser | Q2 | GET /api/rooms (per-room mode/players/phase/canJoin from static instance Sets, dispose-safe reads, players-desc sort) + client LIVE MATCHES panel section with delegated JOIN buttons reusing the hosted join path + late-join spawn protection (invulnUntil 3s when matchState==='playing', legacy 1s otherwise) (docs/PRD-live-matches.md). NOTE: parallel session's R8-R10 commits swept the GameRoom/GameScene hunks; endpoint+tests+panel landed here (74787e5) | FR-MP-06 | DONE | master | 74787e5 | TDD: test/roomsApi.test.mjs (two-room phases/counts/sort/canJoin, late-join >=2.5s vs countdown <=1.5s grace); multiplayer.test.mjs whitelist extended for landed magnet type. Full gate green — `npm run check` 100 files ok, `npm test` 174 tests pass; live probe listed own waves room in /api/rooms; Playwright e2e GREEN waves+world+pvp zero console errors | 2026-08-22 |
| 2.9 Adaptive music director | Q2 | Pure tier state machine (audio/musicDirectorCore.js: CALM/EXPLORE/COMBAT/THREAT via hp/damage-window/elite/streak signals, 1.2s hysteresis hold, bar-quantized transitions) + WebAudio binding (MusicDirector.js: pad reuse, 55Hz pulse bass, single looping noise hats, THREAT riser sweep; crossfades through sound.master so mute/volume keep working); narrow GameScene hooks (docs/PRD-music-director.md) | FR-JUICE-03 | DONE | master | 77f1b99 | TDD: test/musicDirector.test.mjs 6 unit (12-row truth table, hold/pause/promotion semantics, boundary wraparound). Full gate green — `npm run check` 96 files ok, `npm test` 165 tests pass; real-browser Playwright e2e GREEN waves+world+pvp, zero page/console errors with director live | 2026-08-22 |
| 2.8 Presence panel | Q2 | Cross-room presence registry (server/presence.js) wired via 2-line hooks in all 4 room types + lobby redirect mode flips; GET /api/players {count,players[{name,mode}]}; client #online-panel (leaderboard-panel pattern, 5s poller, mode-colored dots, offline state) + localStorage Recent Allies (cap 20, arena-session snapshot) (docs/PRD-presence.md). Joint landing with parallel session's XP orb drops (b40264f) | FR-SOC-01 | DONE | master | b40264f | TDD: test/presence.test.mjs 5 unit (upsert/dedupe/sort/defaults) + test/presenceApi.test.mjs integration (two room types listed, leave drops count, lobby untouched). Combined-tree gate green — `npm run check` 91 files ok, `npm test` 150 tests pass, `npm run smoke` 8/8; live probe :3999 — joined player visible in /api/players with mode waves | 2026-08-22 |
| 2.7 Kill streaks + combat juice | Q2 | Server-authoritative streaks (2.5s window, milestones 3/5/10/15/25) via shared/sim/streaks.js wired in GameRoom/LocalRoom/ArenaRoom(PvP); milestone-only killStreak broadcast; client trauma-shake (trauma², decay 1.5/s), render-only hit-stop (50ms hit / 110ms kill), pitch-ascending blip, gold 1.5x float, #streak-toast (docs/PRD-kill-streaks.md). Joint landing with parallel session's enemy archetypes (750844e) | FR-GAME-02 | DONE | master | 750844e | TDD: test/streaks.test.mjs 6 unit (window lapse/boundary, independent players, resets) + test/streaksIntegration.test.mjs (one broadcast @3 kills, in-window silence, death reset, LocalRoom byte-equal parity, match-reset clear). Combined-tree gate green — `npm run check` 86 files ok, `npm test` 133 tests pass; transient suite hang during concurrent-session test runs resolved after archetypes stabilized | 2026-08-22 |
| R3 VS-genre research + enemy archetypes | gameplay track | Vampire Survivors genre study -> ranked backlog (docs/vampire-survivors-research.md); Enemy Archetypes per docs/PRD-enemy-archetypes.md: deterministic per-(wave,slot) Rusher/Tank tags from wave 3 via shared/sim/markArchetypes (online/offline parity by construction), hp composition ceil(base*hpMul), chase speedMul, Tank knockback x0.25 through combatBook.knockbackAgainst (elite Bulwark immunity wins), elite slot stays archetype-free, client tint+scale under elite treatment; Shooter archetype deferred | FR-GAME-02 | DONE | master (joint tree with 2.7) | e4956e5 (research/docs), 750844e (implementation) | TDD red-first: test/archetypes.test.mjs 8 unit (table shape, wave<3 purity, determinism sweep, mark/reset semantics, elite-slot exclusion); knockback extension pinned in simCombatBook.test.mjs (Tank 0.25x, immunity composes); waves.test.mjs extended (wave-3 tag+hp exact asserts, LocalRoom byte-equal parity, pause-wall-aware clear flow); elitesIntegration updated for composed non-elite HP. Full gate green — `npm run check` 86 files ok, `npm test` 133/133, `npm run smoke` 8/8 | 2026-08-22 |
| R4 Shooter archetype | gameplay track | Third archetype per PRD-enemy-archetypes.md Shooter cycle: deterministic k===3 tag from wave 5 (waves 3-4 melee-only ramp); approach/kite/hold band AI (preferred 6u, kite 4u) mirrored byte-for-byte in both sims; fires 'arrow' ownerIsPlayer=false on synced 2.5s volleys -> revived enemy-owned branch in shared/sim/projectileLoop (any living player, corpses skipped, enemies immune) routed through rooms' damagePlayer (block/shield authoritative); zero schema/client changes (generic arrow render) | FR-GAME-02 | DONE | master | (this commit) | TDD red-first: archetypes.test.mjs extended (table entry, wave-gate selector sweep, hpMul 1.0 stamping); simProjectileLoop dead-code pin SANCTIONED-FLIPPED into live contract (hit-once, corpse skip, ttl-in-open); waves.test.mjs direct-drive wave-5 blocks (server live-fire + LOCAL parity volley); elitesIntegration composed-hp predicate extended. Full gate green — `npm run check` 93 files ok, `npm test` 160/160, smoke 8/8 | 2026-08-22 |
| R5 Magnet power-up | gameplay track | Fourth power-up per PRD-magnet.md: while held, orbs within 8u drift to holder at 10u/s via shared/sim/magnetPull.js pullOrbs (clamp-no-overshoot, first-holder-wins, strict-radius); runs BEFORE collection in both sims' updatePickups so pulled orbs pay same tick; count 3->4 all types spawn; client violet horseshoe mesh; zero timer work (generic effects tick). Synergy: pulls charged kill-orbs too | FR-GAME-02 | DONE | master | (this commit) | TDD red-first: magnetPull.test.mjs 5 unit (clamp/direction/radius/order/no-op safety); waves.test.mjs direct-drive integration (converge+pay budget, out-of-radius freeze, expiry stop, four-type spawn pin). Full gate green — `npm run check` 96 files ok, `npm test` 165/165, smoke 8/8 | 2026-08-22 |
| R6 Wave finale | gameplay track | Bounded run arc per PRD-wave-finale.md: wave.finaleWave=12 (0=endless), advancing past it ends co-op VICTORY (gameover + WorldState.victory, winner fields empty); both advance paths guarded identically; shared reset clears victory; client overlay 'THE HORDE IS BROKEN — VICTORY!' branch w/ optional victory cue; death endings never set the flag | FR-GAME-02 | DONE | master | (this commit) | TDD red-first: waves.test.mjs server block (finaleWave=2 config-flip: two clear/advance cycles -> gameover+victory, playAgain restores playing+victory=false) + LOCAL parity mini-block (manual-step mirror). Full gate green — `npm run check` 96 files ok, `npm test` 165/165, smoke 8/8 | 2026-08-22 |
| R7 Finale Boss (Warlord) | gameplay track | Boss-tier affix per PRD-wave-finale follow-up: FINALE_BOSS_AFFIX 'Warlord' (hp x3, kb-immune, 25% vampiric, spd 1.15) registered in affixByName but EXCLUDED from the rotation; finaleBossFor(n, finaleWave) supersedes regular elite on the finale wave (independent of %5 cadence); slot-0 exclusion extended to bosses so archetype mults never restamp boss hp; LocalRoom byte-parity; reuses ALL shipped hooks (knockback/vampiric/payKillXp/client render) | FR-GAME-02 | DONE | master | (this commit) | TDD red-first: elites.test.mjs +2 (rotation-exclusion sweep 1..200, exact-wave boss selection incl. endless-off); waves.test.mjs direct-drive blocks (server: wave-6 Warlord + composed hp + clean wave-7; LOCAL parity); stun-pinned fan setup kills a P1.4-class rusher-crosses-arc flake. Full gate green — `npm run check` 96 files ok, `npm test` 167/167, smoke 8/8 | 2026-08-22 |
| R8 Career stats (meta lite) | gameplay track | PRD-career-stats.md: per-player {runs,victories,bestWave,bestScore} via shared/sim/careerStats.js recordRun, hooked ONCE in GameRoom.endMatch (victory/death/timed/daily all covered); careerUpdate broadcast; client overlay career line; persistence.loadPlayer PENDING-OVERLAY so endMatch's merge + daily finalize's immediate re-read cannot clobber each other; offline LocalRoom documented out-of-scope v1. Also: fixed combat.test PvP section for peer's 3s late-join grace (PRD-live-matches.md) — wait on server-truth invuln expiry | FR-RET-01 | DONE | master | (this commit) | TDD red-first: careerStats.test.mjs 4 unit (fresh/accumulate/ties/sanitize); pendingOverlay.test.mjs 2 pins (queued-visible pre-flush, cross-key survival); waves.test finale block asserts persisted JSON post-flushAll (runs/victories/bestWave). Full gate green — `npm run check` 100 files ok, `npm test` 174/174, smoke 8/8 | 2026-08-22 |
| R9 Readability pass | gameplay track | Research lessons #10/#11: enemy-owned arrows (Shooter) render red-shifted at creation (ownerIsPlayer is immutable per projectile); charged kill-orbs read gold w/ hot emissive — charge now mirrored onto OrbState.charge by shared orbDrops (chargeForKill stamps, drainCharge reverts) so the client polls schema truth per-frame with no extra callbacks; LocalRoom free (same module) | FR-GAME-02 | DONE | master | (this commit) | TDD: orbDrops.test.mjs +1 pin (charge mirror/revert); full gate green — `npm run check` 100 files ok, `npm test` 175/175, smoke 8/8; visuals verified via CI e2e boot | 2026-08-22 |
| R10 Finale surge | gameplay track | The finale wave deploys the ENTIRE remaining pool (placed away from players, formula HP) in both sims — mirrored daily-bonus pattern; boss + archetypes compose on top (slot-0 exclusions intact); test fan geometry made cone-safe via adaptive spacing (P1.4-class lesson generalized) | FR-GAME-02 | DONE | master | (this commit) | TDD red-first: waves.test.mjs server+LOCAL full-pool pins; fan-spacing fix after surge exposed ±86° > 60° cone. Full gate green — `npm run check` 102 files ok, `npm test` 179/179, smoke 8/8 | 2026-08-22 |
| R11 Boss/surge announcements | gameplay track | Warlord toast restyled ('☠ BOSS — WARLORD — SURVIVE THE SURGE', red border, 6s) via eliteSpawn boss flag (server+LocalRoom payloads); backlog statuses synced in research doc | FR-GAME-02 | DONE | master | (this commit) | Client-only diff + payload flag; full gate green — `npm run check` 102 files ok, `npm test` 179/179, smoke 8/8 | 2026-08-22 |
| R12 Shooter windup telegraph | gameplay track | Research lesson #11 (legible deaths): two-phase volley in both sims — attack anim + anim-override window WINDUP_MS=500 BEFORE the arrow spawns (volley state {at,told} in the per-room fire map, reset each shot); integration pins anim-visible-with-zero-projectiles then delayed fire; LocalRoom byte-parity | FR-GAME-02 | DONE | master | (this commit) | TDD red-first: waves.test shooter block extended (windup-visible/no-projectile-yet assert). Full gate green — `npm run check` 102 files ok, `npm test` 179/179, smoke 8/8 | 2026-08-22 |
| R13 BUGFIX: daily victory finalize | gameplay track | Cross-system review bug: winning the finale in daily/weekly rooms skipped finalizeDailyRun entirely (victory branch called endMatch directly -> gameover froze state -> finalize's own guard returned) — winners lost streak/blob/dailyResult. Fix: challenge-mode victory routes through finalizeDailyRun BEFORE endMatch (finalize ends the match itself); waves mode unchanged; weekly inherits via dispatch | FR-RET-01 | DONE | master | (this commit) | TDD red-first: daily.test.mjs victory block (finaleWave=2 two-cycle run -> gameover+victory + dailyResult broadcast + persisted daily.streak=1/lastPlayed/career.victories). Full gate green — `npm run check` 103 files ok, `npm test` 180/180, smoke 8/8 | 2026-08-22 |
| R14 Weekly-victory parity pin | gameplay track | weeklyIntegration.test.mjs victory block: finale win in weekly mode -> gameover+victory + dailyResult(kind=weekly) + persisted weekly blob keyed to utcWeekKey + career.victories — pins that R13's finalize-first fix covers BOTH challenge modes via the dispatch | FR-RET-01 | DONE | master | (this commit) | Block green solo + full gate `npm run check` 103 files ok, `npm test` 180/180 | 2026-08-22 |
| R15 Docs sync | gameplay track | README: power-ups table gains MAGNET, waves-mode row reflects bounded runs, new 'Gameplay systems' section (elites/archetypes/kill-drops/finale/career/gauntlets) | n/a (docs) | DONE | master | (this commit) | `npm run check` 103 files ok (docs-only) | 2026-08-22 |
| R16 Career unlock tints | gameplay track | Cosmetic tier from persisted bestWave (6/9/12 -> tiers 1/2/3) via shared tierForCareer; PlayerState.tier derived once at join (loadPlayer); client nametag accent colors + stars, schema-polled per frame; zero balance impact; unlock toast deferred | FR-RET-01 | DONE | master | (this commit) | TDD red-first: careerStats.test.mjs tier thresholds (10 asserts); waves.test join-derivation pin (seeded bestWave 9 file -> tier 2). Full gate green — `npm run check` 103 files ok, `npm test` 181/181, smoke 8/8 | 2026-08-22 |
| R17 Unlock toast | gameplay track | Closes the R16 deferral: careerUpdate rows now carry server-derived tier (+ live PlayerState.tier refresh so nametag accents update immediately); client compares vs last-seen tier across matches and toasts 'COSMETIC TIER N UNLOCKED' on increase for the local player | FR-RET-01 | DONE | master | (this commit) | Client diff + row field; `npm run check` 106 files ok, `npm test` 181/181, smoke 8/8 | 2026-08-22 |
| R18 Banner-overlap fix | gameplay track | Audit find: #daily-results (z16) rendered UNDER the gameover overlay (z20 blur) exactly when streak lines matter — victory/defeat both. Raised to z23 (above gameover 20 / upgrade 22, below reconnect 25); click-dismiss + 8s timer unchanged | n/a (UI fix) | DONE | master | (this commit) | `npm run check` 107 files ok; daily suite green; CI e2e covers boot flows | 2026-08-22 |
| R19 Toast-stack stagger | gameplay track | Audit find #2: streak-toast (top 84) overlapped daily-results (top 64) when a kill-streak milestone landed on the finale-winning kill tick; staggered to 118px | n/a (UI fix) | DONE | master | (this commit) | `npm run check` 107 files ok | 2026-08-22 |
| R20 ARCHITECTURE sim-module sync | gameplay track | ARCHITECTURE.md gains a src/shared/sim/ inventory (14 modules: leveling/combatBook/shopEffects/projectileLoop/matchPhases/dailyRun/weeklyRun/elites/archetypes/orbDrops/magnetPull/careerStats/streaks/achievements) with ownership one-liners — the doc predated the entire P1.3-extraction + hackathon-cycle module layer | n/a (docs) | DONE | master | (this commit) | `npm run check` 109 files ok (docs-only) | 2026-08-22 |
| R21 Offline career parity | gameplay track | Pages-fallback runs now fold endings into a localStorage career via the SAME shared recordRun/tierForCareer math as the server path (fail-soft on storage errors); tier-up toasts + overlay line work offline; closes PRD-career-stats documented out-of-scope | FR-RET-01 | DONE | master | (this commit) | Client diff guarded by isLocalRun; `npm run check` 109 files ok, `npm test` 195/195, smoke 8/8, CI e2e covers offline boot flow | 2026-08-22 |
| R22 Persisted tier baseline | gameplay track | lastTierSeen hydrates from localStorage ('ashfall-lasttier') and re-persists on every observation — first-session tier-ups now toast correctly instead of being swallowed by the empty runtime baseline; fail-soft on storage errors | FR-RET-01 | DONE | master | (this commit) | Client diff; `npm run check` 109 files ok, `npm test` 195/195 | 2026-08-22 |
| R23 Offline achievements parity | gameplay track | Pages-fallback endings now evaluate the peer's pure achievements engine client-side over the localStorage blob (career predicates fully active; daily/weekly fail harmlessly without blobs); new unlocks toast via the shared banner. Import merged with peer's existing achievementById line | FR-RET-01 | DONE | master | (this commit) | Client diff; `npm run check` 109 files ok, `npm test` 195/195, smoke 8/8, CI e2e covers offline boot | 2026-08-22 |
| R24 Boss health bar | gameplay track | Dedicated top-center bar while the Warlord lives: #boss-bar element + CSS, HUD-tick poll over enemy views (elite==='Warlord' && hp>0), fill mirrors applyElite max-hp math via static waveEnemyHp import; hides on death/wave end | FR-GAME-02 | DONE | master | (this commit) | Client-only diff; `npm run check` 113 files ok, `npm test` 195/195, smoke 8/8, CI e2e boot coverage | 2026-08-22 |
| R25 Charged-orb chime | gameplay track | SoundManager.charged() C-major arpeggio fired from the existing orb-look poll on the charged->plain flip (collection moment); works online + offline; distinct from base pickup() | n/a (audio polish) | DONE | master | (this commit) | `npm run check` 113 files ok, `npm test` 203/203, smoke 8/8 | 2026-08-22 |
| R26 README refresh | gameplay track | Career/achievements/checkpoints/gauntlet-victory coverage in the public gameplay section | n/a (docs) | DONE | master | (this commit) | `npm run check` 113 files ok (docs-only) | 2026-08-22 |
| R27 Boss-bar daily-mode accuracy | gameplay track | Self-audit find: boss bar computed max from plain waveEnemyHp, but DAILY-mode bosses carry modifier-scaled hp -> fill pinned at 100%. Fix: applyElite stamps EnemyState.bossMaxHp for every elite; client prefers synced field with waveEnemyHp fallback | FR-GAME-02 | DONE | master | joint 58eb11c | TDD red-first: elites.test.mjs +2 stamps; gate green 121 files / 205+ tests | 2026-08-22 |
| R28 Suite-unblock fix | gameplay track | weeklyObjectives.test.mjs never exited post-results (dangling gracefulShutdown) hanging node --test + master verify; added repo-standard process.exit(0) after results | FR-OPS-02 | DONE | master | 185eb80 | Solo exits <60s; full gate WITH file included: 121 files ok, 211/211 | 2026-08-22 |
| R24 Stability soak (post-R31) | gameplay track | P1.4-protocol double consecutive full-suite run after 16 same-day cycles: 195/195 x2 zero flakes; hosted CI verify+e2e green streak across the push train | FR-FAIR-03 | DONE | master | n/a (verification) | Evidence: two consecutive green gates this cycle + CI success rows for R28/R29/R30 pushes | 2026-08-22 |
| R25 Weekly banner streak fix | gameplay track | Audit find: weekly results rows rendered a misleading 'streak x1' (template defaulted the absent field; weekly deliberately has no streak). Segment now renders only when r.streak != null | n/a (UI fix) | DONE | master | (this commit) | `npm run check` 113 files ok, weekly+daily suites green | 2026-08-22 |
| R26 New-best-wave callout | gameplay track | endMatch derives wasBestWave from the PRIOR record (post-fold best can't distinguish new vs tie) and ships it in careerUpdate rows; client toasts 'NEW BEST WAVE REACHED' for the local player on true improvements only | n/a (UX polish) | DONE | master | (this commit) | `npm run check` 114 files ok, `npm test` 203/203, smoke 8/8 | 2026-08-22 |
| R20 Systems-compose pin | gameplay track | test/compose.test.mjs: flagship chain in one real room — shooter kill -> corpse-charged orb (schema mirror) -> magnet convergence -> base+charge payout; elite kill composes payKillXp doubling into its drop; guards future drift across the now-10 interacting systems | FR-FAIR-03 | DONE | master | (this commit) | Full gate green — `npm run check` 107 files ok, `npm test` 187/187 | 2026-08-22 |
| R21 Burn-kill attribution | gameplay track | Fairness hole closed: firewave DoT kills paid NOTHING (silent clamp). tickBurns now detects fatal ticks from SOURCED burns and credits via the same chain as every kill — killScore, payKillXp (dropOrb routing + elite doubling), creditKill streak hook, enemy_killed log, onEliteKill burst/fuse. registerProjBurn carries proj.ownerSid; unsourced burns keep legacy silent cleanup; both rooms mirror | FR-GAME-02 | DONE | master | (this commit) | TDD red-first: simCombatBook.test.mjs +3 (sourced credit, elite double+Volatile arm, legacy-silent). Full gate green — `npm run check` 108 files ok, `npm test` 193/193, smoke 8/8 | 2026-08-22 |
| R22 Ranged-block fairness pin | gameplay track | Guard contract extended to ranged: a blocking player facing a Shooter takes ZERO arrow damage with blocked feedback (same damagePlayer path as melee/PvP); test isolates the shooter (park+freeze others) and polls for impact-registered 'blocked' instead of fixed sleeps | FR-GAME-02 | DONE | master | (this commit) | waves.test shooter block extended; full gate green — `npm run check` 108 files ok, `npm test` 195/195 | 2026-08-22 |
| 2.24b Share card image rendering | gameplay track | PRD-share-card.md "Cycle: image rendering": layoutShareCard(card) pure 800x450 geometry+colors (title/stat rows/footer) + chooseShareMode capability ladder in shareCard.js; GameScene._renderShareCanvas draw-only offscreen renderer consuming layout verbatim; SHARE click walks native Web Share files -> ClipboardItem png copy -> cycle-21 text copy with SHARED/IMAGE COPIED/COPIED/COPY FAILED acks and AbortError cancel handling | FR-RET-01 | DONE | master | (this commit) | TDD red-first: shareCardImage.test.mjs 6 pins (deep-equal determinism, bounds+distinct-y rows, title/footer contract, all 4 chooser boolean combos). Full gate green — `npm run check` 133 files ok, `npm test` 235 pass / 0 fail, smoke 8/8 | 2026-08-23 |
| Block-guard arrow flake fix | gameplay track | waves.test block-guard: live-fire phase left an enemy arrow mid-flight that landed before blocking engaged (CI 32654162989 red proof, zero-loss assert failed under runner load); fix clears in-flight enemy projectiles when arming the guard so only post-arm volleys reach it | n/a (test) | DONE | master | (this commit) | node --test test/waves.test.mjs x6 consecutive green; full gate — `npm run check` 143 files ok, `npm test` 264 pass / 0 fail | 2026-08-23 |
| FR-HUD-05 HP denominator fix | gameplay track | dogfood pass caught knight (150 base hp) rendering a 150% hp bar + late-arming vignette — all three client sites divided by CONFIG.player.maxHp=100; replaced with shared effectiveMaxHp(character, upgrades) | FR-HUD-05 | DONE | master | (this commit) | Red-first e2e: assert_hud_feedback hp-fill <=100% pin failed live (150%) -> fixed -> waves flow GREEN. Full gate — `npm run check` 143 files ok, `npm test` 264 pass / 0 fail | 2026-08-23 |
| FR-HUD-04 Damage direction wedge | gameplay track | dirAngleDeg pure evaluator (clockwise-from-up CSS rotation); both rooms emit targeted damaged {x,z} on successful hits only; #dmg-dir arrow rotates at hit time and fades 0.7s — client-only rendering | FR-HUD-04 | DONE | master | (this commit) | TDD red-first: test/damageDir.test.mjs 4 pins (cardinal angles, range+determinism sweep, degenerate no-NaN, LocalRoom emission with shield-silence control). Full gate green — `npm run check` 143 files ok, `npm test` 264 pass / 0 fail, smoke 8/8 | 2026-08-23 |
| FR-RET-03 Replay reset + time survived | gameplay track | kills now reset in shared resetMatchState (bug from FR-GAME-03: counters carried across replays); formatRunTime M:SS pure renderer + Time share-card line; GameScene run clock stamped on wave-1 playing transition, "SURVIVED M:SS" on gameover | FR-RET-03 | DONE | master | (this commit) | TDD red-first: test/runSummary.test.mjs 3 pins. PLUS daily.test flake fix: clear helper drained cards post-intermission -> pause wall/death could end runs pre-victory (3/5 fail on clean HEAD) — fixed to poll-until-clear + mid-clear drains + hp floor, 8/8 green. Full gate — `npm run check` 141 files ok, `npm test` 260 pass / 0 fail, smoke 8/8 | 2026-08-23 |
| FR-RET-02 Wave-clear orb vacuum | gameplay track | during intermission both rooms treat every living player as a full-map magnet (pullOrbs radius Infinity over the shipped magnet math); leftover orbs converge and pay via the normal proximity pickup — playing state unchanged | FR-RET-02 | DONE | master | (this commit) | TDD red-first: waves.test vacuum block (RED: converged-within-budget fail x2 pre-wiring; control pin: distant orb static while playing) + magnetPull Infinity-radius clamp unit pin. Full gate green — `npm run check` 140 files ok, `npm test` 257 pass / 0 fail, smoke 8/8 | 2026-08-23 |
| FR-GAME-03 Kill counter | gameplay track | PlayerState.kills synced field; shared combatBook increments killer.kills at both kill sites (resolveEnemyHit + tickBurns fatal sourced burn) so GameRoom+LocalRoom credit identically by construction; hudText shows kills N, share card gains Kills line when provided | FR-GAME-03 | DONE | master | (this commit) | TDD red-first: test/killCounter.test.mjs 4 pins (direct kill/survivor split, fatal burn credit, unattributed no-credit, share-card gating + text-once). Full gate green — `npm run check` 140 files ok, `npm test` 256 pass / 0 fail, smoke 8/8 | 2026-08-23 |
| Arena shield-pickup flake fix | gameplay track | CI run 32646704386 red proof: arena.test kill assert failed 10!==0 because the victim grabbed a seeded shield power-up mid-test (shield_absorb logged) and absorbed the killing blow — orbs were parked, power-ups were not. Fix parks state.powerUps at (40,40) alongside orbs and strips shield effects before the exact kill/score/projectile asserts | n/a (test) | DONE | master | (this commit) | node --test test/arena.test.mjs x5 consecutive green; full gate green — `npm run check` 139 files ok, `npm test` 252 pass / 0 fail | 2026-08-23 |
| E2E HUD feedback asserts | gameplay track | test/browser.test.py assert_hud_feedback: real-browser pins that all four cycle surfaces are DRIVEN not merely present — xp-fill width %, wave label /^WAVE \d/ + fill %, danger opacity 0 at full hp, settings strip + volume slider; wired into the waves flow after bloom | n/a (test) | DONE | master | (this commit) | Live-verified locally: waves flow GREEN incl. new asserts (xp 20%, WAVE 1/12, vol 100). Red-proof by mutation (wave-label driver disabled -> exact AssertionError, restored -> GREEN). Full gate green — `npm run check` 139 files ok, `npm test` 252 pass / 0 fail | 2026-08-23 |
| FR-UX-01 Settings strip | gameplay track | resolveFxSettings/loadFxSettings/saveFxSettings pure in shared/sim/fxSettings.js (injected storage, absent-vs-junk volume semantics, corrupt-blob defaults, particleScale/shakeScale derivation); GameScene wraps spawnBurst so all burst sites scale from one point, shake+trauma multiply by shakeScale; bottom-right volume slider + REDUCED FX checkbox persisted to localStorage and live-applied | FR-UX-01 | DONE | master | (this commit) | TDD red-first: test/fxSettings.test.mjs 5 pins (defaults, clamp/coerce matrix, reduced-fx scales, injected-storage round-trip, corrupt JSON+junk fields). Full gate green — `npm run check` 139 files ok, `npm test` 252 pass / 0 fail, smoke 8/8 | 2026-08-23 |
| FR-HUD-03 Wave-progress chip | gameplay track | waveChip(wave,finaleWave) pure evaluator in shared/waves.js (N/M label, exact pct, isFinale flag, endless + clamp handling); #wave-chip label+track under xp bar, red when finale; reads static SERVER config so no schema/room changes | FR-HUD-03 | DONE | master | (this commit) | TDD red-first: test/waveChip.test.mjs 4 pins (exact fraction, finale flag at N==M, beyond-finale clamp, endless shape). Full gate green — `npm run check` 137 files ok, `npm test` 247 pass / 0 fail, smoke 8/8 | 2026-08-23 |
| FR-HUD-02 Low-HP danger vignette | gameplay track | lowHpFx(hp,maxHp) pure evaluator (arms AT 30%, linear ramp to full at 5%, dead/degenerate off); #danger edge-only radial overlay pulsing via intensity*sine per frame — client-only, no sim/schema changes | FR-HUD-02 | DONE | master | (this commit) | TDD red-first: test/lowHpFx.test.mjs 4 pins (healthy/dead off, boundary arms at exactly 30% w/ intensity 0, linear midpoint + clamps, zero maxHp safety). Full gate green — `npm run check` 136 files ok, `npm test` 243 pass / 0 fail, smoke 8/8 | 2026-08-23 |
| FR-HUD-01 XP progress bar | gameplay track | xpProgress(level,xp) pure evaluator in shared/progression.js (bracket into/need/pct clamped 0..1) beside xpForLevel; #xp-bar/#xp-fill in index.html styled after the cooldown bars; GameScene sets fill width per HUD tick from synced {level,xp} — client-only, zero server changes | FR-HUD-01 | DONE | master | (this commit) | TDD red-first: test/xpProgress.test.mjs 4 pins (exact threshold math, near-full bracket, clamp both ends, monotonic+determinism). Full gate green — `npm run check` 134 files ok, `npm test` 239 pass / 0 fail, smoke 8/8 | 2026-08-23 |
| R17 Perf gate: surge soak | gameplay track | test/perfSurge.test.mjs — worst-case finale wave (boss + full pool + shooters + players scattered) soaked 6s sampling GameRoom.stats.lastTickMs; budgets median<10ms/p95<25ms/max<45ms catch pathological regressions, not jitter | FR-MP-04 evidence | DONE | master | (this commit) | Measured: median 0.11ms, p95 0.25ms, max 1.32ms (n=288) — fixed-pool architecture leaves ~200x headroom; `npm run check` 104 files ok | 2026-08-22 |
| 2.6 Elite affixes | Q2 | Every 5th wave spawns one elite (1.8x scale, red tint, gold hp border) with deterministic affix cycle Swift/Bulwark/Vampiric/Volatile via shared/sim/elites.js; vampiric heal, volatile delayed AoE w/ fuse telegraph ring, bulwark knockback immunity, double killScore+XP burst, eliteSpawn toast; identical wiring in GameRoom + LocalRoom (docs/PRD-elite-affixes.md) | FR-GAME-01 | DONE | master | 78a0c5e | TDD: test/elites.test.mjs 5 unit + test/elitesIntegration.test.mjs (online wave5 exactly one Swift @ ceil(base*hpMul), wave6 clean, LocalRoom parity, Swift 1.6x closure, vampiric heal exact, volatile fuse+AoE 25, bulwark knockback skip). Full gate green — `npm run check` 81 files ok, `npm test` 25 files / 114 tests pass, `npm run smoke` 8/8; live probe :3999 waves room playable, wave1 zero elites as designed | 2026-08-22 |
| 2.4 Air-gapped asset vendoring | Q2 | Vendored three/importmap selectable via env, offline deploy recipe | FR-OPS-08 | TODO | TBD | - | Gate: blocked-outbound container serves playable Waves session; smoke covers vendored mode | - |
| 3.1 Theme token system | Q3 | Token file + tenant resolution driving login/HUD/overlays | FR-WL-01, FR-WL-04 | TODO | TBD | - | Gate: two tenants one process show different branding; fallback theme on unknown tenant | - |
| 3.2 Embed mode + postMessage | Q3 | iframe/script snippets, ?embed=1 chrome-less mode, origin-checked score messages | FR-WL-02 | TODO | TBD | - | Gate: Playwright parent-page e2e receives gameover score; non-whitelisted origins dropped | - |
| 3.3 Branded lobby & sponsor slots | Q3 | Sponsor/logo slots on login/countdown/results driven by tokens | FR-WL-03 | TODO | TBD | - | Gate: renders clean with zero sponsors; e2e screenshots with sponsor set on all three surfaces | - |
| 3.4 Custom game config API | Q3 | Create-time room options with validation clamps (mode, rounds, difficulty, seed, caps) | FR-MP-05 | TODO | TBD | - | Gate: invalid configs rejected; two differently-configured rooms coexist; defaults unchanged when omitted | - |
| 3.5 Scale-out pilot under load | Q3 | Redis presence across N processes + load balancer; measured report committed | FR-MP-04 | TODO | TBD | - | Gate: >= 500 concurrent / >= 4 processes, cross-process lobby redirect proven, p99 tick < 25ms | - |
| 4.1 SSO story + SCIM-lite | Q4 | Azure AD/Okta recipes; provision/deactivate -> profile flag enforced at join/auth | FR-ID-03, FR-ID-02 | TODO | TBD | - | Gate: deactivated identity cannot join or resume; guest path unaffected | - |
| 4.2 License tiers + billing hooks | Q4 | MIT core vs enterprise packaging matrix; license-key boot hook (no phone-home in OSS build) | FR-BIZ-01, FR-BIZ-02, FR-BIZ-03 | TODO | TBD | - | Gate: OSS build asserts absence of license checks; enterprise build rejects invalid key | - |
| 4.3 On-prem Helm chart | Q4 | Chart for server + optional Redis, healthcheck wired, backup/restore runbook executed | FR-OPS-05, FR-PERSIST-05 | TODO | TBD | - | Gate: kind-cluster deploy passes /healthz readiness; two replicas pass cross-process matchmaking check | - |
| 4.4 SOC2-lite controls checklist | Q4 | Controls doc mapped to security questionnaires with artifact links | FR-OPS-06, FR-OPS-04 | TODO | TBD | - | Gate: >= 80% questionnaire coverage without "not applicable"; each control links enforcing artifact | - |
| 4.5 Template gallery v1 | Q4 | Curated tenant starter packs (theme + room config + license-clean notes) with loader | FR-CONTENT-03, FR-WL-01, FR-MP-05 | TODO | TBD | - | Gate: each template plays via one config change; NOTICE generator passes; >= 5 templates listed | - |
| 4.6 1.0 release | Q4 | Tag 1.0.0, release notes mapping FR ids to evidence, migration guide, Docker image | FR-BIZ-01, FR-BIZ-02 (stretch: FR-FAIR-04) | TODO | TBD | - | Gate: full suite green on release commit; image smoke-tested; every PRD FR row accounted for | - |

## Currently in flight

- **UI revamp session (concurrent, separate agent):** tracked in `docs/artwork-ui-rework/TODO.md` (phases A/U) per the research committed in a868e54. It touches client files `index.html`, `src/main.js`, `src/client/NatureDressing.js`. Do not rebuild that artwork/UI work inside roadmap phases, and do not edit those three files from roadmap branches without coordinating — expect mid-air collisions otherwise. Roadmap phases that touch client visuals (3.1, 3.3, 4.5) must rebase onto that session's landed commits first.

## Session summary — infinite-loop gameplay hackathon (2026-08-22)

One continuous agent-driven product cycle (research -> backlog -> TDD ->
gate -> land, repeating) layered the full Vampire-Survivors-style genre core
onto Ashfall while peer sessions shipped enterprise-track systems in the
same tree (weekly gauntlet ca95762, presence b40264f/7ae1d7b spectate,
achievements 604cc26, offline checkpoints 5ea1919, music director, OIDC in
flight). Coordination protocol held all day: claim via PRD files, targeted
staging, monitor-then-land on shared files, joint landings credited both
sides.

| Theme | Cycles | Key commits |
|-------|--------|-------------|
| Genre research + backlog | R3 | e4956e5 (VS study, ranked ideas, PRDs) |
| Elite affixes support + shooter archetype | R4-R5 | 750844e (joint), 6009c2c windup telegraph |
| Kill-drop orb economy | A | b40264f (joint), PRD b9c85cb |
| Magnet power-up | C | 3d42bff |
| Finale arc: victory + surge + Warlord boss | D-E-H | 761d5c8, 4eff0f4, 8268a8a |
| Career meta: stats/tints/toasts/offline parity | F-N-Y | f2be3cf, f45a477, d1953aa, 4abf254 |
| Fairness fixes: victory finalize, burn attribution, ranged block | K-U-V | 6d0dab9, a6fefe7, 10fb248 |
| Readability: hostile arrows, gold orbs, chime, boss bar, trauma shake | G-AG-AJ | 4c605db, f888fa0, 5e374a7, 89a5da7 |
| UI audit fixes: banner z-order x2, weekly streak text, new-best callout | R-S-AM-AO | fb4cdfb, 55af191, 77af692, e9d5b2c |
| Stability: perf gate, soak evidence, orb-noise guards x3 | AC-AD-T | 8595a6d, 1c2ca84, a9dc254/21cd6bb/806c04a |
| Docs syncs: README, ARCHITECTURE sim inventory, backlog statuses | M-W-AA | c2ad807, 5003523, cddf492/5a2c84d |

Final state at last push: master `e9d5b2c` — `npm run check` 114 files ok,
`npm test` **203/203** (88 tests added today), smoke 8/8, CI verify+e2e
green streak. Zero TODO/FIXME markers repo-wide. Audit findings shipped as
fixes: daily-victory finalize skip (winners lost streaks), banner z-order,
weekly fake-streak text, two missing-module CI breaks repaired with peers.

## Session summary — Q1 SHIPPED (2026-08-22)

Q1 "Harden & License" is complete and live on origin master. Every phase went
through the full cycle: plan -> review -> TDD red/green -> local gate
(check + test + smoke) -> worktree -> merge -> green CI -> this tracker.

| Item | Result |
|------|--------|
| CI gate (P1.2) | verify job: npm ci -> check -> test -> smoke (real server boot + WebSocket join). First green run 32516367093 |
| MIT license (P1.1) | LICENSE + package.json + README section, guard test red-first. 763a0ab / CI 32517647752 |
| Shared-sim extraction (P1.3) | D2 leveling, D5+D4+D3 combatBook/shopEffects, D6 projectileLoop, D7+D8 matchPhases (+D1 activateWave stretch). GameRoom.js 1330->1141 (-189), LocalRoom.js 892->780 (-112); ~300 duplicated lines now single-source; 2 sanctioned alignments pinned by tests. Slices: ffd1f1f, cd24327, 611f26a, e1d44d8 |
| Flaky-test elimination (P1.4) | Audit (3 clean runs + 8 findings) -> deterministic red proofs for combat margin + world persistence leak; fixes #1-#6 + 4 extra latent flakes; 12/12 stress green; arena round-waits poll server state. fe15c33 / CI 32530964445 |
| Browser e2e in CI (P1.5) | e2e job boots production server, drives test/browser.test.py in real Chromium (waves/world/pvp GREEN). d5b08c3 / CI 32534675813 |

Final state at close: master d5b08c3 - check 75 files OK, 106/106 tests,
smoke 8/8, CI verify+e2e green. Redundant one-off probes removed
(d185d85); zero cozodb/datadog logic repo-wide (rg + ast-grep verified).

## Next up (Q2 Platform Foundations - see ROADMAP.md)
1. OIDC/OAuth2 login option (guest stays default)
2. PlayerStore adapter interface + Postgres implementation (JSON default)
3. Admin API + GDPR export/delete + audit log

## Definition of done for every phase

A phase may only move to DONE when every box is satisfied:

- [ ] TDD followed: failing test written first (red), then implementation (green)
- [ ] `npm run check && npm test` green on the final commit of the branch
- [ ] Live local smoke passed: `npm run serve` up + `curl /healthz` ok + tools/smoke.mjs run
- [ ] Real-browser check performed when any client file was touched (per EXPANSION_PLAN.md checklist)
- [ ] This file updated: Status, Commit hash, Verification evidence, Date
- [ ] Merged to origin master
- [ ] CI green on GitHub for the merge commit
